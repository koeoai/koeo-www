/**
 * Content type definitions for centralized marketing content.
 * These types ensure type safety when working with content across the site.
 * Re-exported from the parent types.ts for convenience.
 */

export * from "../types";
